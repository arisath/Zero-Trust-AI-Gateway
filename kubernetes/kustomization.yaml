apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  # OpenLDAP (user directory — must start before auth-service)
  - openldap/secret.yaml
  - openldap/configmap.yaml
  - openldap/pvc.yaml
  - openldap/deployment.yaml
  - openldap/service.yaml
  # Redis
  - redis/pvc.yaml
  - redis/deployment.yaml
  - redis/service.yaml
  # Ollama
  - ollama/pvc.yaml
  - ollama/deployment.yaml
  - ollama/service.yaml
  # Auth Service
  - auth-service/secret.yaml
  - auth-service/deployment.yaml
  - auth-service/service.yaml
  # AI Gateway
  - ai-gateway/configmap.yaml
  - ai-gateway/secret.yaml
  - ai-gateway/deployment.yaml
  - ai-gateway/service.yaml
  - ai-gateway/ingress.yaml
  - ai-gateway/hpa.yaml
